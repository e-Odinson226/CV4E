# AGENTS.md — pointer

This repo's agent instructions live in **`CLAUDE.md`** (routing) and
**`docs/EgoVault/CLAUDE.md`** (the authoritative working rules: where notes go, the frontmatter
schema, the append-only decision log, Q&A formatting, and the report rule).

Read `CLAUDE.md` first. It is deliberately thin and routes to `docs/EgoVault/_INDEX.md` for the
current state.

Applies to every agent — Claude Code, Gemini CLI, or otherwise. Keep one set of rules; do not
add tool-specific instruction files outside version control.
